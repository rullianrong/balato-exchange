<%= form_with url: quote_path, method: :get do |form| %>
  <div class="field">
    <%= form.label :symbol %>
    <%= form.text_field :symbol %>
  </div>

  <div class="actions">
    <%= form.submit 'Search' %>
  </div>
<% end %>
